# Google Cloud Build ignore file
# Only exclude large/unnecessary files for build

# Large dependencies and build outputs
node_modules/
.next/
.vercel/
coverage/
.nyc_output/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# VCS
.git/

# Environment files (but keep .env.example)
.env.local
.env.production.local
.env.staging.local

# Keep everything else - package.json, source code, configs, etc.
