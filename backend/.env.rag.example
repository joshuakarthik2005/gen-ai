# RAG Snippet Filtering - Environment Variables
# Add these to your backend .env file or deployment configuration

# ============================================
# OPTIONAL: Discovery Engine Filtering
# ============================================
# Enable user-level filtering (recommended for production)
# Uncomment and set after updating Discovery Engine schema
# RAG_FILTER_METADATA=user_id

# Document-level filtering field (default: document_id)
# RAG_FILTER_DOCUMENT_ID=document_id

# ============================================
# Discovery Engine Configuration
# ============================================
# These should already be configured
# RAG_ENGINE_PROJECT=demystifier-ai
# RAG_ENGINE_LOCATION=global
# RAG_ENGINE_ID=synapseragengine_1758347548138

# ============================================
# Fallback Behavior
# ============================================
# Enable sample snippets when no results found (default: true)
# RAG_ENABLE_FALLBACK=true

# Allow mock data when Discovery Engine unavailable (default: false)
# RAG_ALLOW_MOCK=false

# ============================================
# NOTES
# ============================================
# 1. The system works WITHOUT these variables (uses post-search filtering)
# 2. Setting RAG_FILTER_METADATA improves performance (filters at Discovery Engine level)
# 3. Requires Discovery Engine schema to have filterable fields (see RAG_FILTERING_SETUP.md)
# 4. Safe to deploy with or without these variables
