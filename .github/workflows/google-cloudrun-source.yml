# This workflow will deploy source code on Cloud Run when a commit is pushed to# This workflow will deploy source code on Cloud Run when a commit is pushed to

# the "main" branch.# the "main" branch.

##

# To configure this workflow:# To configure this workflow:

##

# 1. Enable the following Google Cloud APIs:# 1. Enable the following Google Cloud APIs:

##

#    - Artifact Registry (artifactregistry.googleapis.com)#    - Artifact Registry (artifactregistry.googleapis.com)

#    - Cloud Build (cloudbuild.googleapis.com)#    - Cloud Build (cloudbuild.googleapis.com)

#    - Cloud Run (run.googleapis.com)#    - Cloud Run (run.googleapis.com)

#    - IAM Credentials API (iamcredentials.googleapis.com)#    - IAM Credentials API (iamcredentials.googleapis.com)

##

#    You can learn more about enabling APIs at#    You can learn more about enabling APIs at

#    https://support.google.com/googleapi/answer/6158841.#    https://support.google.com/googleapi/answer/6158841.

##

# 2. Create and configure a Workload Identity Provider for GitHub:# 2. Create and configure a Workload Identity Provider for GitHub:

#    https://github.com/google-github-actions/auth#preferred-direct-workload-identity-federation.#    https://github.com/google-github-actions/auth#preferred-direct-workload-identity-federation.

##

#    Depending on how you authenticate, you will need to grant an IAM principal#    Depending on how you authenticate, you will need to grant an IAM principal

#    permissions on Google Cloud:#    permissions on Google Cloud:

##

#    - Artifact Registry Administrator (roles/artifactregistry.admin)#    - Artifact Registry Administrator (roles/artifactregistry.admin)

#    - Cloud Run Source Developer (roles/run.sourceDeveloper)#    - Cloud Run Source Developer (roles/run.sourceDeveloper)

##

#    You can learn more about setting IAM permissions at#    You can learn more about setting IAM permissions at

#    https://cloud.google.com/iam/docs/manage-access-other-resources.#    https://cloud.google.com/iam/docs/manage-access-other-resources.

##

# 3. Change the values in the "env" block to match your values.# 3. Change the values in the "env" block to match your values.



name: 'Deploy to Cloud Run from Source'name: 'Deploy to Cloud Run from Source'



on:on:

  push:  push:

    branches:    branches:

      - '"main"'      - '"main"'



env:env:

  PROJECT_ID: 'demystifier-ai' # Your Google Cloud project ID  PROJECT_ID: 'demystifier-ai' # Your Google Cloud project ID

  REGION: 'asia-south1' # Your deployment region  REGION: 'asia-south1' # Your deployment region

  SERVICE: 'legal-backend' # Your Cloud Run service name  SERVICE: 'legal-backend' # Your Cloud Run service name



jobs:jobs:

  deploy:  deploy:

    runs-on: 'ubuntu-latest'    runs-on: 'ubuntu-latest'



    permissions:    permissions:

      contents: 'read'      contents: 'read'

      id-token: 'write'      id-token: 'write'



    steps:    steps:

      - name: 'Checkout'      - name: 'Checkout'

        uses: 'actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332' # actions/checkout@v4        uses: 'actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332' # actions/checkout@v4



      # Configure Workload Identity Federation and generate an access token.      # Configure Workload Identity Federation and generate an access token.

      #      #

      # See https://github.com/google-github-actions/auth for more options,      # See https://github.com/google-github-actions/auth for more options,

      # including authenticating via a JSON credentials file.      # including authenticating via a JSON credentials file.

      - id: 'auth'      - id: 'auth'

        name: 'Authenticate to Google Cloud'        name: 'Authenticate to Google Cloud'

        uses: 'google-github-actions/auth@f112390a2df9932162083945e46d439060d66ec2' # google-github-actions/auth@v2        uses: 'google-github-actions/auth@f112390a2df9932162083945e46d439060d66ec2' # google-github-actions/auth@v2

        with:        with:

          workload_identity_provider: 'projects/144935064473/locations/global/workloadIdentityPools/github-actions/providers/github'          workload_identity_provider: 'projects/144935064473/locations/global/workloadIdentityPools/github-actions/providers/github'

          service_account: 'github-actions@demystifier-ai.iam.gserviceaccount.com'          service_account: 'github-actions@demystifier-ai.iam.gserviceaccount.com'



      - name: 'Deploy to Cloud Run'      - name: 'Deploy to Cloud Run'

        uses: 'google-github-actions/deploy-cloudrun@33553064113a37d688aa6937bacbdc481580be17' # google-github-actions/deploy-cloudrun@v2        uses: 'google-github-actions/deploy-cloudrun@33553064113a37d688aa6937bacbdc481580be17' # google-github-actions/deploy-cloudrun@v2

        with:        with:

          service: '${{ env.SERVICE }}'          service: '${{ env.SERVICE }}'

          region: '${{ env.REGION }}'          region: '${{ env.REGION }}'

          # Deploy from the backend folder where the API code is located          # Deploy from the backend folder where the API code is located

          source: './backend'          source: './backend'



      # If required, use the Cloud Run URL output in later steps      # If required, use the Cloud Run URL output in later steps

      - name: 'Show output'      - name: 'Show output'

        run: |-        run: |-

          echo ${{ steps.deploy.outputs.url }}          echo ${{ steps.deploy.outputs.url }}
